{
  "name": "demo-api-server",
  "version": "1.0.0",
  "description": "Demo API server that implements the 'flash' x402 scheme.",
  "main": "dist/app.js",
  "scripts": {
    "start": "tsc && node dist/app.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sleepysort",
  "license": "MIT",
  "dependencies": {
    "express": "^5.1.0",
    "typescript-ioc": "^3.0.5",
    "typescript-logging": "^2.2.0",
    "typescript-logging-category-style": "^2.2.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "typescript": "^5.9.2"
  }
}
